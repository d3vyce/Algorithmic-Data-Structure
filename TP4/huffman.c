#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct noeud {
    unsigned char c;
    int occurence;
    int code;
    int bits;
    struct noeud *gauche, *droit;
}noeud;

int main(int argc, char* argv[]) {

    return 0;
}